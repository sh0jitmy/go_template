# Secrets Example (encrypt with: sops -e secrets.example.yml > secrets.enc.yml)
s3:
  bucket: your-bucket-name
  prefix: app
  endpoint: https://s3.tky01.sakurastorage.jp
  region: jp-east-1
  access_key_id: your-access-key
  secret_access_key: your-secret-key
